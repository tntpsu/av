# Process Health Report — 2026-05-31

Generated by: tools/nightly/process-health/run.sh
Source: data/reports/improvement_log.json (14 entries, 2026-03-22 → 2026-04-17)

---

## WHERE ISSUES ORIGINATE

```
═══════════════════════════════════════════════════════
Design          ████████████████████████████  64%  (9)
Implementation  ████████████                 21%  (3)
Testing         ████                          7%  (1)
Tooling         ████                          7%  (1)
```

Design-phase decisions account for nearly two-thirds of all rework. Implementation
bugs and testing gaps each contribute ~1 in 5 and ~1 in 14 respectively.

---

## DESIGN SUB-CATEGORIES

```
DESIGN SUB-CATEGORIES (9 entries)
═══════════════════════════════════════════════════════
Proxy-approximation     ████████████████████  44%  (4)
  (speed tables, distance→time, scalar collapse of spatial curvature)
Signal routing          █████                 11%  (1)
  (at-car vs lookahead reference frame mismatch)
Missing state machine   █████                 11%  (1)
  (continuous FF active across all driving phases)
State discretization    █████                 11%  (1)
  (binary gates on continuous curvature signal)
Temporal coupling       █████                 11%  (1)
  (control rate coupled to perception rate)
Missing physics model   █████                 11%  (1)
  (fixed damping on speed-varying plant gain)
```

**Proxy-approximation is the dominant design failure pattern.** Four of nine design
issues shared the same root: a hand-tuned parameter or table was used as a proxy for
a physics quantity that could have been computed directly. In each case the proxy
failed at boundary conditions — exactly the conditions the physics formula handles
correctly by construction.

Examples:
- Speed threshold + curvature guard → replace with κ×v² (lateral accel budget)
- Speed lookup table for PP floor → replace with physics formula
- Distance threshold for curve arm → replace with time-to-curve
- Per-frame scalar curvature reduction → replace with spatial velocity profile

---

## DETECTION EFFICIENCY

```
═══════════════════════════════════════════════════════
Avg iterations to root cause:    2.3
Issues caught by tooling:        100%  (14/14)
Issues requiring manual trace:     0%  (0/14)

Trend: IMPROVING
  First half (entries 1–7):   avg 3.1 iterations
  Second half (entries 8–14): avg 1.4 iterations
```

Every issue was eventually surfaced by a diagnostic tool rather than purely manual
inspection. Tooling (PhilViz, mpc_pipeline_analysis, analyze_oscillation_root_cause.py,
steering decomposition trace, /iterate bisect) is doing real work. The 2.3× reduction
in detection iterations from first to second half reflects the tooling investment
compounding — each new trace tool made the next issue faster to diagnose.

High-iteration outlier (5 iterations): MPC reference alignment (2026-04-05). The
correlation diagnostic existed but wasn't run until iteration 5 because the symptom
(oscillation on curves) had several plausible explanations. Suggests that a contract
test asserting corr(mpc_e_lat, -lateral_error) > 0.95 would have caught this at merge.

---

## PREVENTION THEMES

```
═══════════════════════════════════════════════════════
Theme                         Count  Example
Physics-first formula rule      4    "derive from κ×v² before adding threshold"
Phase/arch design rule          4    "phase-gate all FF terms by default"
CI gate / regression test       4    "E2E record required for tuning changes"
Input validation                1    "plausibility filter on GT lane boundaries"
Stability analysis gate         1    "compute gain margin vs speed before deploy"
```

Three prevention themes are tied at 4 entries each — no single intervention dominates.
However the **physics-first** and **CI gate** themes have the highest expected leverage:
- Physics-first rules prevent proxy-approximation issues upstream (before code is written)
- CI gates catch regressions that slip through design review (the q_lat bundling incident)

---

## TOP 3 PROCESS IMPROVEMENTS

```
════════════════════════════════════════════════════════
1. Proxy-approximation design gate
   Pattern: 44% of design issues, 29% of all issues, stem from proxy parameters
            that approximate a physics quantity derivable from first principles.
   Action:  Add a design-review checklist item: "Does this threshold depend on
            ≥2 physics quantities (speed, curvature, time)? If yes, derive the
            combined formula and document it before writing code." Apply to any
            new YAML parameter added to av_stack_config.yaml.
   Impact:  Prevents the most frequent failure mode at zero tooling cost. The
            κ×v², physics PP floor, and time-based arm fixes each took 1–3
            extra iterations; design-time formula derivation collapses this to 0.

2. Pre-merge E2E validation record for tuning changes
   Pattern: The mpc_q_lat bundling incident (+82.5 pts recovered, 4 tracks
            regressed) happened because a numeric tuning change was buried in a
            feature commit with explicitly deferred E2E validation. 1 entry
            but maximum deduction among all entries.
   Action:  Require that any commit touching _MPC_WEIGHT_AUTO_DERIVE_PARAMS or
            av_stack_config.yaml numeric constants includes an E2E validation
            record (min: highway_65 + one curve track, ≥60s each). Consider a
            lightweight pre-commit grep that detects these changes and prompts.
   Impact:  Prevents score regressions that would otherwise take a full /iterate
            bisect cycle (1–3 runs × 60s each) to diagnose. The single q_lat
            incident cost more rework than any other entry.

3. Contract test: mpc_e_lat correlation at merge
   Pattern: MPC reference alignment took 5 iterations (highest in the log)
            because the correlation diagnostic existed but wasn't in the merge
            path. A contract test would have flagged the -0.71 correlation
            at CI time.
   Action:  Add pytest test: on any MPC-active golden recording, assert
            corr(mpc_e_lat, -scoring_lateral_error) > 0.85 for curve segments
            (curvature > 0.01). This is a 10-line test using existing HDF5
            golden recordings. Also validate regime transition symmetry
            (max |Δsteering| < threshold at A→B and B→A) while at it.
   Impact:  Would cut the worst-case detection delay from 5 iterations to 0
            (caught at commit time). Covers signal-routing class of issues which
            currently has no automated gate.
```

---

## Notes

- Log covers 2026-03-22 to 2026-04-17 (26 days). No entries after 2026-04-17 —
  either the `/log-fix` discipline has lapsed, or no commits with fixes occurred.
  The CLAUDE.md instruction says "run /log-fix after any commit that fixes an issue."
  If fixes have shipped since 2026-04-17, the log is understating the true entry count
  and skewing the Pareto toward early-period patterns.
- Trend is improving but the data window is narrow (14 entries). Re-run in 4 weeks
  after the log fills in from the active Stage 2 work.
